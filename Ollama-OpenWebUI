export OLLAMA_HOST=0.0.0.0       to make ollama listen on all interaces so the docker container for open_webui can reach it
ollama serve
docker run -d -p 3000:8080 --gpus all --add-host=host.docker.internal:host-gateway -v open-webui/data:/app/backend/data --name open-webui --restat always ghcr.io/open-webui/open-webui:cuda
