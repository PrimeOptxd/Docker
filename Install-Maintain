sudo pacman -S docker docker-compose
sudo systemctl enable docker
sudo usermod -aG docker $USER
   REBOOT

TO STOP:
docker compose stop

TO START:
docker compose start

TO STOP AND DELETE EVERYTHING OTHER THAN DATA:
docker compose down

TO START FROM SCRATCH WITH DATA:
docker compose up -d

TO UPDATE:
cd ~/selfhost/WHATEVER IT IS TO UPDATE
docker compose pull
docker compose up -d

docker ps -a
LIST ACTIVE CONTAINERS

docker images
LIST IMAGES

docker volume ls
LIST ALL VOLUMES

docker system prune -a 
FULL CLEAR EVERYTHING

SET 127.0.0.1 INFRONT OF 8080:8080 SO THAT ONLY CURRENT DEVICE HAS ACCESS TO THE SERVICES

NOTE:
with vpn this doesnt work. you have to make some adjustments 
In the .yml you have to remove ports and add 
network_mode: "host"
then to map a specific port
environment:
 - NAME_port=0000
and to make that only accessible from host and not whole lan you add to firewall (nftables) under loopback
tcp dport 0000 drop
